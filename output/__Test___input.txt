if (c.getDay() == [R]
	    && sched.numCourses([R]) >= courseMaxR) {
    		break;
	}
if ( PLT.hasDatetime( [M W] 1:10~2:25 ) ) {
    myPLT = PLT.getSection( [MW] 1:10~2:25 );
    mylist.add( myPLT );
}