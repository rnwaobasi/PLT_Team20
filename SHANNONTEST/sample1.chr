schedule sched;
courselist CL = generateCourses("input.txt");
foreach course c in CL {
	//time t = c.getStartTime();
	//print("Time is ", t);`
	if (c.getStartTime() > 10:00) {
		sched.add(c);
		print("should have added ", c);
	} else {}
}
print(sched);

